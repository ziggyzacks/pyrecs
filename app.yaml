runtime: custom
env: flex
entrypoint: gunicorn --timeout 180 --bind :$PORT app:app --worker-class sanic.worker.GunicornWorker

runtime_config:
  python_version: 3

manual_scaling:
  instances: 1
resources:
  cpu: 1
  memory_gb: 3
  disk_size_gb: 10